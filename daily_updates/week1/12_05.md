# May 12, 2026

**Total Combined Hours:** 5 hours (0.5h Skin Preferences, 4h Library Embed POC, 0.5h Menu Bar Research)

## Work

### Skin Preferences — Alignment Fix (0.5h)

- **Refined PR [#16446](https://github.com/mixxxdj/mixxx/pull/16446):** Addressed CI/CD failures in the pull request enabling QML skins as a preference option.
- The skin preference integration now correctly bootstraps the QML path when a QML-type skin is selected in Developer Mode, without breaking the legacy `--qml` flag fallback.

### Legacy Library Embed in LateNightQML — POC (4.5h)

- **Goal:** Embed the existing Qt Widgets-based Mixxx library panel inside the QML skin as a proof-of-concept, establishing functional parity without a full rewrite.
- **Branch:** [`PoC-Legacy-Library-QML-Integration`](https://github.com/xARSENICx/mixxx/tree/PoC-Legacy-Library-QML-Integration)
- **Approach:** Exploring `QQuickPaintedItem` as a portal to render and forward input events to the legacy `QWidget`-based library tree.
- **Research & Investigation:**
  - Studied how `MixxxMainWindow` wires up the library widget and how it interacts with `CoreServices`.
  - Investigated `QmlLegacyLibraryItem` (the existing stub in `src/qml/`) to understand what bridge infrastructure is already in place.
  - Traced the `Library` → `LibraryControl` → `WTrackTableView` call chain to identify the minimal set of objects needed to instantiate a working library panel outside of `MixxxMainWindow`.
- **Progress:** Successfully identified the dependency chain and began prototyping the widget embedding. The POC is ongoing.

### Research — QML Menu Bar (misc)

- Investigated `Qt.labs.platform.MenuBar` as a path to bring a native menu bar to the QML skin.
- **Findings:**
  - Available since Qt 5.8 — compatible with Mixxx's minimum Qt 5.12 requirement.
  - Renders a **native OS-level menu bar** (macOS global menu, Windows titlebar, KDE/GNOME D-Bus), not a QML-drawn widget — correct behavior.
  - Marked as `labs` (unstable API), which is a risk for a long-lived project.
  - The main challenge is not the QML API itself but **bridging all existing `WMainMenuBar` actions** from C++ into a QML-consumable model.
  - Tabled for now; not blocking current POC work.

## Weekly Goals (May 11 - May 17)

- [ ] ⏳ **[Priority]** Embed legacy library in QML using the `QQuickPaintedItem` approach.
- [x] **[Priority]** Move QML from CLI flag to Skin Preferences (hidden in Developer Mode).
- [ ] **[Priority]** Pull and verify PR [#16095](https://github.com/mixxxdj/mixxx/pull/16095) to enable Qt 6.10 via vcpkg on macOS.
- [ ] Benchmark QML startup time with a single-hotcue button.
- [ ] Implement `Theme.qml` color validation and SVG existence testcase.
- [ ] Research Qt 6.10 SVG improvements.
